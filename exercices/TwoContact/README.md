# FirstContact

Demonstrates a simple MQTT client using paho-mqtt. Run:
python client.py --role pong
python client.py --role ping

or by runing the shell script start_clients
 source s
 start_clients.sh
Default broker: localhost. Topic: lab/first_contact/hello.
